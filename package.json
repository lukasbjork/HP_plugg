{
  "name": "hogskoleprovet-studiesite",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "scraper",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"blue,green\" \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "scrape": "npm run build --workspace=scraper && node scraper/dist/index.js",
    "build": "npm run build --workspace=client",
    "lint": "eslint \"**/*.{ts,tsx}\" --ignore-pattern node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,json,css,md}\"",
    "seed:turso": "npx ts-node --project tsconfig.base.json scripts/seed-turso.ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5"
  }
}
